# taskflow
This is TaskFlow — one real product that naturally uses all 4 skills: The signup/login form is the entry gate — real JS validation with email regex, password match check, min-length, live red/green field states.  it actually stores and reads the account. 
